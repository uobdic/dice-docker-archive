Simple set up for testing of storage.xml with https://github.com/dmwm/PHEDEX/blob/master/Utilities/TestCatalogue.
To be run via
```
docker run -v <path to>/SITECONF:/opt/SITECONF -t -i kreczko/phedex-test
```